<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="/table.css">
    <title>Document</title>
    
</head>
<body>

    <div class="table-container">
        <table>
            <caption>Library Books</caption>
            <thead>
                <tr>
                    <th>Book ID</th>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for( book of data ) { %>
                <tr>
                    <td><%= book.book_id %></td>
                    <td><%= book.title %></td>
                    <td><%= book.author %></td>
                    <td>
                        <form method="get" action="/books/<%= book.book_id %>">
                            <button>Issue book</button>
                        </form>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>

</body>
</html>
