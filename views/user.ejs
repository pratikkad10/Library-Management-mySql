<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Username Form</title>
    <style>
        
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            color: white;
            background: rgb(23, 13, 30);
            background: linear-gradient(90deg, rgba(23, 13, 30, 1) 12%, rgba(74, 18, 14, 1) 100%);
        }

        /* Form container styles */
        .form-container {
            text-align: center;
            padding: 20px;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.1); /* Semi-transparent for contrast */
        }

        /* Input and submit button styling */
        input[type="text"], input[type="submit"] {
            padding: 10px;
            margin: 10px 0;
            font-size: 1em;
            border: none;
            border-radius: 5px;
        }

        input[type="text"] {
            width: 200px;
            background-color: #fff;
            color: #000;
        }

        input[type="submit"] {
            background-color: #02366ebd;
            color: #fff;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #0057b39c;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <form action="/books/username" method="POST">
            <input type="text" id="username" name="username" placeholder="Enter username" required>

            <input type="hidden" name="book[book_id]" value="<%= bookDetails.book_id %>">
            <input type="hidden" name="book[title]" value="<%= bookDetails.title %>">
            <input type="hidden" name="book[author]" value="<%= bookDetails.author %>">

            <input type="submit" value="Submit">
        </form>
    </div>

</body>
</html>
